---
// Features section component
---

<section class="features">
  <div class="container">
    <h2>Why Choose Wampoon?</h2>
    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">🚀</div>
        <h3>Easy Setup</h3>
        <p>
          Get your WAMP development environment up and running in minutes with
          Wampoon's streamlined installation process.
        </p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">⚡</div>
        <h3>High Performance</h3>
        <p>
          Wampoon is optimized for speed and efficiency, ensuring your development workflow is smooth and productive.
        </p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">🔧</div>
        <h3>Simple Management</h3>
        <p>
          Easily start, stop, and configure Apache and MariaDB with Wampoon's intuitive interface.
        </p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">🛠️</div>
        <h3>Developer Tools</h3>
        <p>
          Wampoon includes essential development tools and utilities to enhance your local development experience.  
        </p>
      </div>      
      <div class="feature-card">
        <div class="feature-icon">🌟</div>
        <h3>Open Source</h3>
        <p>
          Wampoon is built with transparency and community collaboration at its core, completely free to use.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    background: var(--color-card);
    padding: 6rem 0;
    position: relative;
  }

  .features h2 {
    text-align: center;
    font-size: 3rem;
    margin-bottom: 4rem;
    color: var(--color-text);
    font-weight: 800;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 3rem;
  }

  .feature-card {
    text-align: center;
    padding: 2.5rem 2rem;
    border-radius: 16px;
    background: var(--color-background);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    transition: all 0.3s ease;
    border: 1px solid var(--color-border);
    position: relative;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 48px rgba(58, 143, 208, 0.4);
    border-color: var(--color-primary);
  }

  .feature-card::after {
    content: "🐾";
    position: absolute;
    top: 1rem;
    right: 1rem;
    font-size: 1.2rem;
    opacity: 0.3;
    pointer-events: none;
    color: var(--color-primary);
  }

  .feature-icon {
    font-size: 3.5rem;
    margin-bottom: 1.5rem;
    display: block;
  }

  .feature-card h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--color-text);
    font-weight: 700;
  }

  .feature-card p {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }
</style>
